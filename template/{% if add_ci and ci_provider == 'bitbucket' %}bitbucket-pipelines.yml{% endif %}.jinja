image: node:{{ node_version }}-bookworm

pipelines:
  default:
    - step:
        name: Lint and Build
        caches:
          - node
        script:
          - npm ci
          - npm run format:check
          - npm run lint
          - npm run build
